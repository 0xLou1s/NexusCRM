packages:
  - "apps/*"
  - "packages/*"

allowBuilds:
  esbuild: true
  sharp: true
  unrs-resolver: true
  msw: false
  # Telemetry that ships with @nestjs/swagger. Denied, not merely unapproved.
  "@scarf/scarf": false
